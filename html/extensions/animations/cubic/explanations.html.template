<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>3DCG: Cubic Easing Function</title>
    <%= stylesheets('3dcg', 'box2') %>
    <%= scripts('jquery', 'jquery-ui', 'underscore', 'ace/ace', 'source-editor') %>
    <style>
      img.large {
        width: 60%;
      }

      video {
        margin: 10px auto;
      }
    </style>

    <script type="text/x-mathjax-config">
      MathJax.Hub.Config({tex2jax: {inlineMath: [['$','$'], ['\\(','\\)']]}});
    </script>
    <script type="text/javascript" async src="https://cdn.mathjax.org/mathjax/latest/MathJax.js?config=TeX-AMS_CHTML"></script>
  </head>

  <body>
    <header>
      Cubic Easing Function
    </header>
    <div id="contents">
      <%= overview(mutually_exclusive_with: ['animations/quadratic', 'animations/quintic'], reading_material: ['math/animations', 'design/animations/basics', 'math/easing-functions']) %>
      <section>
        <h1>Preview</h1>
        <%= raytrace_movie 'demo' %>
        <%= source 'demo.chai' %>
      </section>
      <section>
        <h1>Implementation</h1>
        <aside class="boxed2">
          <p>
            Create files <code>math/functions/easing/cubic-function.cpp/.h</code> and implement
            <code>cubic_in</code>, <code>cubic_out</code> and <code>cubic_inout</code>.
            Use <code>linear-easing-function.h/.cpp</code> as a guide. You'll need to find the formulae yourself. 
          </p>
          <p>
            Don't forget to add a binding for the scripting language. Update <code>scripting/math-module</code> by adding an extra <code>IMPORT</code>s to
            the <code>EasingLibrary</code> and extra <code>BIND</code>s to <code>add_easing</code>.
          </p>
        </aside>
      </section>
    </div>
  </body>

  <script>
    function initialize()
    {
      SourceEditor.initialize();
    }

    $( initialize );   
  </script>
</html>
