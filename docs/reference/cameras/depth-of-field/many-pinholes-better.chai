// image

use("shared.chai")


var camera = Cameras.depth_of_field( [ "eye": pos(0,0,5),
                                       "look_at": pos(0,0,0),
                                       "eye_size": 0.2,
                                       "eye_sampler": Samplers.stratified(10,10)] )

render(camera, "many-pinholes-better.bmp")
