= 2D Cartesian Coordinate System

In a 2D Cartesian coordinate system is defined by its origin point stem:[O] and two perpendicular axes stem:[X] and stem:[Y].
The location of each point in the 2D plane can be expressed using coordinates stem:[(x, y)].

image::2d-cartesian.png[align="center"]
