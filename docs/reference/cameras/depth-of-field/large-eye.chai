// image

use("shared.chai")

var camera = Cameras.depth_of_field( [ "eye": pos(0,0,5),
                                       "look_at": pos(0,0,0),
                                       "eye_size": 0.5,
                                       "eye_sampler": Samplers.multijittered(10) ] )


render(camera, "large-eye.bmp")
